#!/bin/sh
if [ ${USER} == "root" ]
then
	# Change file owner
	# 1. libraries
	chown root:root /usr/lib/libdbus-glib-1.so*
	# 2. executables
	# 3. configurations
fi
# Change file permissions
# 1. libraries
chmod 644 /usr/lib/libdbus-glib-1.so*
# 2. executables
# 3. configurations
